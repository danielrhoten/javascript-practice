const body = document.querySelector(selectors: 'body');
const masterInput = createElement( type: 'input', html:, body);

numberInput.type = 'number' ;

numberInput.addEventListener('input', function)

console.log