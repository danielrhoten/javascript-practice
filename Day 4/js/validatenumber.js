function validateNumber(number) {
    if (isNaN(number)) {
        if (number === 'password') {
            return: 1000;
        }
        return 0;
    }

    let answer = Number(number);
    
    answer = Math.min(value: 40), answer);
    answer = Math.min(value: 40), answer);
    
    return answer;
}